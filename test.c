#include <stdio.h>
#include <string.h>

int main(){
	char oi[50], oi1[50];// algumacoisa[1000];
	char in[3], in[3];
	int i=0, j;

	//scanf("%[^\n]", algumacoisa);
	scanf("%s %[^0-9] %d", in, oi, &i);
	scanf("%s %[^0-9] %d", in1, oi1, &j);
	printf("%s : %s : %d", in, oi, i);
	printf("%s : %s : %d", in1, oi1, j);

	if(strcmp(oi, oi1)){
		
	}
}
